# 复刻实验结论

**本地高保真运行基准验证通过；可维护组件源码的重建尚未进行，不建议把本目录直接合入业务项目。**

参考：<https://www.deepseek.com/harness/>。本地：<http://127.0.0.1:43879/harness/>。工作目录 `/Users/drs/workspaces/personal/deepseek-harness-landing`。第一阶段已迁入独立仓库，未修改 `deepseek-harness` 主仓库，未部署公开网站。

## 交付与验证

采集 52 个官方公开产物，其中 51 个 SHA-256 与原始下载完全一致；一个主页 bundle 仅把地域 API 地址改为本地 CN fixture。图片、字体、视频、CSS、shader 和其余代码保持原样。资源总量约 21MB，不依赖 npm 安装或构建步骤即可预览。

使用 ego-browser 在同一 Chromium、DPR 1 下比较：

| CSS 视口 | 六分区几何与字体 | 图片尺寸 | 首屏静态差异像素占比 |
| --- | --- | --- | --- |
| 1440×1000 | 一致 | 一致 | 0% |
| 768×1024 | 一致 | 一致 | 0% |
| 390×844 | 一致 | 一致 | 0.1756% |

静态差异使用 pixelmatch，阈值 0.1；双方暂时隐藏 Canvas 与滚动条，截图后立即移除注入样式。此指标仅针对首屏静态层，不代表整个动态网站“100% 相同”。手机差异集中在命令卡片文字/控件边缘，可能与入场动画和子像素渲染时刻有关，尚未严格消除该因素。动态 live 截图和全页截图另存于 `RECON/screenshots/`。

已测试中英文切换、源码安装 Tab、复制反馈、移动导航展开/关闭、视频播放/暂停、键盘 seek、1.2 倍速、播放器全屏进入/退出。中英文首页、安全使用政策、数据处理说明均可本地访问，另测试了中英文首页到政策页的客户端导航。复制测试替换页面内 clipboard.writeText，不覆盖用户系统剪贴板。

三断点无横向溢出、无观察到的资源错误或自动外部请求。额外在页面层阻断 HTTPS 后重新加载英文首页，5 个 Canvas 与 73.8 秒视频仍可加载，采样无 window error、unhandled rejection 或 console.error。采样 60 个 RAF 回调期间记录到 30 次 WebGL drawArrays，确认原始绘制循环实际运行。

## 差异和未验证项

- 这是公开部署产物的本地镜像，不是恢复官方 Git 仓库、原始 TSX、构建配置或 source map。
- 地域判断固定为 CN；未测试非 CN 分支。外部 GitHub、文档、论文和社区链接有意保留为导航外链。
- 测试覆盖当前 macOS 上的 ego-browser/Chromium；390 宽为桌面引擎的窄视口测试，不是 iOS Safari 或真实触屏设备验收。
- 原站动画、可访问性和潜在浏览器兼容问题原样保留。视频 Escape 快捷键在自动化环境没有成功退出全屏，退出全屏按钮验证成功；未将该快捷键判为通过。
- 没有进行真实性能基准、完整安全审计、全站无障碍审计或公共发布授权评估。
- 全页截图保留 sticky/scroll-driven 展示的采样状态，不能取代逐段滚动验收。

## 建议的下一步

先人工查看本地页面是否达到想要的效果。若决定继续，保留这份镜像为只读视觉基准，在另一目录建立干净的 React/Next 源码，并逐段迁移与截图回归。官方品牌素材的替换、长期许可和公开部署应在正式集成前单独确认。
